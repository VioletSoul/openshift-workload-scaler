# openshift-workload-scaler
A Jenkins-based automation pipeline that allows controlled lifecycle management of Kubernetes deployments across multiple environments.

A sanitized XML configuration for the Jenkins job will be added later.
